{
  "name": "senaryo-timeline",
  "version": "1.0.0",
  "private": true,
  "description": "Senaryo Timeline Editörü - Screenplay timeline editor with Firebase collaboration",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "build": "node scripts/build.js",
    "deploy": "npm run build && firebase deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^28.1.0",
    "vitest": "^3.0.0"
  }
}
